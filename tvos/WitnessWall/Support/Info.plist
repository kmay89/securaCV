<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<key>CFBundleDevelopmentRegion</key>
	<string>$(DEVELOPMENT_LANGUAGE)</string>
	<key>CFBundleDisplayName</key>
	<string>Witness Wall</string>
	<key>CFBundleExecutable</key>
	<string>$(EXECUTABLE_NAME)</string>
	<key>CFBundleIdentifier</key>
	<string>$(PRODUCT_BUNDLE_IDENTIFIER)</string>
	<key>CFBundleInfoDictionaryVersion</key>
	<string>6.0</string>
	<key>CFBundleName</key>
	<string>$(PRODUCT_NAME)</string>
	<key>CFBundlePackageType</key>
	<string>APPL</string>
	<key>CFBundleShortVersionString</key>
	<string>$(MARKETING_VERSION)</string>
	<key>CFBundleVersion</key>
	<string>$(CURRENT_PROJECT_VERSION)</string>
	<key>LSRequiresIPhoneOS</key>
	<true/>
	<!-- Export compliance: only Apple's OS crypto (CryptoKit / TLS) with
	     standard algorithms — the exempt category. Same declaration as the
	     iPhone app's Info.plist; without it every TestFlight build parks on
	     the manual "Missing Compliance" questionnaire (tvos-v0.2.2 did). -->
	<key>ITSAppUsesNonExemptEncryption</key>
	<false/>
	<key>UIRequiredDeviceCapabilities</key>
	<array>
		<string>arm64</string>
	</array>
	<key>UILaunchScreen</key>
	<dict/>
	<!-- Build identity for the About/Health panel, stamped by CI on the
	     xcodebuild command line (scripts/stamp_build.sh). The panel reads
	     these, so "which build is on this TV?" is never a guess. -->
	<key>SecuraCVBuildRev</key>
	<string>$(SECURACV_BUILD_REV)</string>
	<key>SecuraCVFirmwareTrain</key>
	<string>$(SECURACV_FW_TRAIN)</string>
	<!-- The Wall talks to a kernel on the LAN over plain http:// — that is the
	     normal case for a hub on your own network, and the only hosts reachable
	     are ones the viewer typed. Scoped to local networking so this is not a
	     blanket ATS exemption: arbitrary public http:// loads stay blocked. -->
	<key>NSAppTransportSecurity</key>
	<dict>
		<key>NSAllowsLocalNetworking</key>
		<true/>
	</dict>
	<key>NSLocalNetworkUsageDescription</key>
	<string>The Witness Wall reads your Canaries' verified record from your hub on this network. It never sees video, and never leaves your network.</string>
	<!-- The service every Canary announces (firmware discovery.cpp). Browsing
	     it is how a fleet with NO hub reaches the Wall: without this key,
	     tvOS silently returns zero Bonjour results and the Wall would keep
	     waiting for a canary.local that nothing claims. -->
	<key>NSBonjourServices</key>
	<array>
		<string>_securacv._tcp</string>
	</array>
</dict>
</plist>
