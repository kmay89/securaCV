{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capabilities for the main SecuraCV Lab window.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "opener:default"
  ]
}
