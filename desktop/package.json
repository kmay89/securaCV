{
  "name": "securacv-flasher",
  "private": true,
  "version": "0.10.0",
  "description": "The SecuraCV Lab as a native desktop app — flash a Canary with no browser and no terminal.",
  "scripts": {
    "tauri": "tauri",
    "dev": "tauri dev",
    "build": "tauri build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
