# Per-flavor staged files — generated by `./setup.sh arduino <watch|dash>`,
# never committed (they differ by flavor, and secrets.h carries credentials).
# The flavor-AGNOSTIC generated sources (canary_display.ino, the shared
# *.cpp/*.h, lv_conf.h, secrets.ci.h) ARE committed and sync-guarded.
pins.h
flavor_config.h
secrets.h
