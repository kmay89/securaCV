# Copy to .env (same directory as docker-compose.yml) and fill in.
# Must match the password set for the "securacv" user in the Mosquitto
# password file (see README "Set up MQTT credentials"). Used by both the
# securacv sidecar and Frigate.
SECURACV_MQTT_PASSWORD=change-me
