# Copy to .env (same directory as docker-compose.yml) and fill in.
# These credentials must match the user created in the Mosquitto password
# file (see README "Set up MQTT credentials").
MQTT_USERNAME=securacv
MQTT_PASSWORD=change-me
