# Canary Hardware Documentation

Build plans and bills of materials for the physical SecuraCV Canary devices —
what to buy, how peripherals wire to the MCU, and which parts are required vs.
optional.

| Document | Description |
|----------|-------------|
| [`bench_bringup.md`](./bench_bringup.md) | **Start here to test it** — minimum shopping list and steps to make a bare XIAO chirp/blink on the bench. |
| [`v1_bench_validation_runbook.md`](./v1_bench_validation_runbook.md) | **The v1 gate's detailed bench procedures** — flash → provision → signed MQTT → verified-✓ in HA, the kernel-pipeline smoke, and the 2–3 board mesh/chirp fleet validation, with pass/fail criteria and required artifacts. The driver sheet that sequences the whole gate is [`docs/V1_BENCH_TEST_RUNBOOK.md`](../V1_BENCH_TEST_RUNBOOK.md). |
| [`enclosure/`](./enclosure/README.md) | **3D-printable enclosures, mounts & workshop tools** (parametric OpenSCAD + ready STLs) for every Canary: WAP box, Vision camera unit, doorbell, Sense radome — plus mounts, a fit-calibration coupon, bench fixture, provisioning dock and 1:1 paper drill templates. **New? Start with its README's "first hour" section.** |
| [`flagship_board_program.md`](./flagship_board_program.md) | **The flagship board program — a custom carrier, and how to certify it cheaply** — the decision record for SecuraCV's first custom PCB ([`boards/canary-witness-s3/`](../../boards/canary-witness-s3/)): why Flux designs the board but does not hold it, why the break-even is **34 units** rather than doc 18's 500 once paid design NRE goes to zero, the one volume quote that decides the whole program (the XIAO must stay above **$11.32/unit**), and why *now* — before the first SDoC — is the cheapest moment to change the board. Status `idea`; nothing is ordered. |
| [`canary_peripheral_build_plan.md`](./canary_peripheral_build_plan.md) | Master build plan & BOM: audible chirp (buzzer), status LED, button/tamper/touch inputs, battery, and enclosure — for both Canary WAP and Canary Vision. |
| [`esp32s3_power_battery_guide.md`](./esp32s3_power_battery_guide.md) | **Power & battery guide** — supply/cable requirements, consumption estimates per firmware power mode, battery chemistry/sizing across temperatures and environments, wiring pitfalls, and the battery health/lifetime telemetry reference. |
| [`solar_power_sizing.md`](./solar_power_sizing.md) | **Solar & battery sizing (reference)** — the canonical "how big a panel/battery?" for every solar Canary: the energy-balance method, real Peak-Sun-Hour data by latitude/season, the panel & battery formulas with honest derating, and the **"right-size, not biggest"** optimization (faucet/bucket/drain balance principle + the real tradeoffs of oversizing each). Worked examples with common maker parts (3000 mAh cells, CN3791 MPPT, why not TP4056). |
| [`cold_weather_envelope.md`](./cold_weather_envelope.md) | **Cold-weather operating envelope (reference)** — how cold each outdoor Canary actually runs: the battery is usually the limit (a LiSOCl₂ primary reaches −55 °C and dodges the 0 °C charge floor; rechargeables can't charge below freezing), the −40 °C silicon floor, why camera nodes bottom out ~−20 °C, the LoRa-pulse HLC-buffer trick, and real winter deployment lessons. Cited by the outdoor concept dossiers. |
| [`canary_vision_getting_started.md`](./canary_vision_getting_started.md) | **Canary Vision getting started** — one clean path from unboxing to a working witness: assemble, load the model, flash, HA discovery, dashboard, and aiming with the boxes-only Aim camera card. |
| [`canary_vision_pro_recamera.md`](./canary_vision_pro_recamera.md) | **Canary Vision Pro (reCamera Pro) — concept** — the flagship tier: a standalone on-device NPU camera integrated with zero new firmware via the existing webhook adapter, claim mapping, what it deliberately doesn't route (VLM/LLM captions), and where the $300 price earns its keep vs. the cheaper Vision path. Includes a real 3D-modeled mount adapter ([`canary_vision_pro_mount.scad`](./enclosure/canary_vision_pro_mount.scad), mesh-verified). |
| [`canary_vision_lite_recamera.md`](./canary_vision_lite_recamera.md) | **Canary Vision Lite (reCamera 2002w) — concept** — the coverage tier: a ~$35 RISC-V (SG2002) WiFi camera on the same zero-new-firmware webhook pattern as Vision Pro, for several mundane spots instead of one flagship chokepoint. Reuses the Vision Pro mount adapter (same reCamera-family mount interface). |
| [`grove_vision_ai_v2_guide.md`](./grove_vision_ai_v2_guide.md) | **Grove Vision AI V2 device guide** — the two USB-C ports explained, Grove I2C port pinouts per host board, loading the initial AI model via SenseCraft, bootloader recovery, and the SSCMA protocol reference. |
| [`canary_sense_ports_and_access.md`](./canary_sense_ports_and_access.md) | **Canary Sense (60 GHz radar) ports + access** — which USB-C port actually flashes (the seated XIAO's, not the power port), why an assembled or sealed unit has to be opened to reach it, and the radome rule for closing it back up. |
| [`we2_module_flasher_bench_test.md`](./we2_module_flasher_bench_test.md) | **WE2 module flasher bench smoke-test** — the first-hardware gate for the Lab's own module flasher: connect the module port, burn the pinned model, prove it with the AT handshake + live preview, with pass/fail criteria and the Vela-compat fallback. Run once on a real module before calling the flasher production-ready. |
| [`acoustic_alarm_bench_test.md`](./acoustic_alarm_bench_test.md) | Bench test for the audible chirp — does the alarm actually alarm. |
| [`usb_drive_bench.md`](./usb_drive_bench.md) | **USB-OTG bench build** (`canary-wap-usbdrive`) — the opt-in S3 image that gives a flashed Canary a branded USB name (`SecuraCV-Canary`) and a read-only "evidence drive" browsable on a computer or an iPhone (USB-C, Files app). The on-device Phase-2 checklist: enumerate, name, drive-in-Files, reflash still works — with the S3-only / FAT32-SD / self-power limits spelled out. |
| [`canary_qr_onboarding.md`](./canary_qr_onboarding.md) | QR onboarding — a new Canary joins by looking at the display's screen. |
| [`csi_sensing_guide.md`](./csi_sensing_guide.md) | WiFi CSI sensing on the bench — hardware-side setup and validation. |
| [`mr60bha2_radar_notes.md`](./mr60bha2_radar_notes.md) | **MR60BHA2 radar deep-dive (Canary Sense)** — the ADT6101P all the way down: antenna/FoV, the wire beyond what we decode, placement physics with citations, the power budget derivation, and six bench flags. Its `SIM:` tables are the drift-gated source for the Sense Lab (`canary-local/sense.html`). |
| [`canary_fence_guard_research.md`](./canary_fence_guard_research.md) | **Fence Guard research dossier (concept)** — the Seeed XIAO ESP32S3 + Wio-SX1262 Meshtastic kit, verified: specs, pin map, power measurements, solar guidance, free pins for the vibration sensor. Feeds the coming-soon concept card and `firmware/projects/canary-fence-guard/`. |
| [`canary_ranger_research.md`](./canary_ranger_research.md) | **Canary Ranger research dossier (concept)** — a low-power Doppler approach witness distilled from studying Samraksh's BumbleBee radar: cheap commodity Doppler (HB100/CDM324/IPM-165) on the Fence Guard Meshtastic kit, battery/solar, reads motion + approaching/receding direction + coarse large-vs-small mover. Replicates the *civilian* "virtual fence" capability and hard-stops the *military* tracking/gait-ID half. Includes how the same classifier applies to Canary Sense's existing 60 GHz radar (the hardware-free win). |
| [`canary_feeder_research.md`](./canary_feeder_research.md) | **Canary Feeder research dossier (concept)** — a solar bird-feeder cam: a perch break-beam/load-cell wakes the camera when a bird lands (not a twitchy PIR that can't even see through glass), on-device "bird visited (coarse bucket)" via XIAO ESP32-S3 Sense / Grove Vision AI V2, local-only snapshot, no raw video sealed. Honest winter-power stack (LiFePO4 + low-temp-cutoff BMS + oversized tilted panel — *no lithium charges below 0 °C*, autonomy carries the freeze) and the sealed-box-fogs-the-lens fix. |
| [`canary_litter_research.md`](./canary_litter_research.md) | **Canary Litter research dossier (concept)** — a cat litter-box **health** witness (distinct from the `litterbox_witness_demo.md` teaching demo): a weight platform under a standard box (load cells + HX711 + auto-tare), vet-backed metrics (body-weight trend, visit patterns, and the male-cat urinary-blockage emergency), framed as non-diagnostic wellbeing signals on Sense's wellbeing channel — no camera, no new claim vocabulary. |
| [`canary_paw_research.md`](./canary_paw_research.md) | **Canary Paw research dossier (concept)** — a camera-free "dog at the door **waiting** to come in" witness, all-weather. Two honest tiers: a trained IP65 paw-button (`ContactStateChange`) and a 24 GHz **FMCW** radar behind a radome with a zone+dwell rule (`PresenceInRestrictedZone`) — must be FMCW not Doppler (a waiting dog is still). Honest about the dog-vs-person radar limit and snow burial; optional owned collar tag for identity. |
| [`canary_clime_research.md`](./canary_clime_research.md) | **Canary Clime research dossier (concept)** — an environmental & power wellbeing witness folding pet temp/thermostat, air quality, and power-outage into one node: SHT4x + species safe-bands + rate-of-change HVAC-failure; air quality via **AirGradient** integration → own Sensirion node (honest thresholds; eCO₂≠CO₂, CO≠CO₂); and outage detection by absence-inference with a Meshtastic egress path (WiFi dies in the outage). Alerts ride the [alert relay](../design/alert_relay.md). |
| [`canary_hearth_research.md`](./canary_hearth_research.md) | **Canary Hearth research dossier (concept)** — a camera-free stove **awareness aid** (explicitly *not* a fire detector): an **MLX90640** thermal array (not AMG8833 — its 80 °C ceiling saturates on cooking) + presence fusion for **unattended-cooking** (the #1 fire cause), and an acoustic **T3/T4 smoke-alarm relay** (`AcousticImpulseInZone`, "we heard your alarm"). Alert-only positioning held hard; flame sensors/gas-leak explicitly out of scope. |
| [`canary_chore_research.md`](./canary_chore_research.md) | **Canary Chore research dossier (concept)** — a stick-on appliance-cycle witness: an **LIS3DH accelerometer** (the Fence Guard part) on the washer/dryer detects running via vibration and **done via absence-inference** (vibration ceases for N min), plus an optional unbalanced-load "washer is walking" nudge. No electrical work, no new claim vocabulary; alerts via the [alert relay](../design/alert_relay.md). |
| [`canary_poolwatch_research.md`](./canary_poolwatch_research.md) | **Canary Poolwatch research dossier (concept)** — a pool-area **awareness aid** (⚠️ *not* a drowning-detection / life-safety device): a Vision-Pro **starlight** camera watching a defined pool zone for a child-sized entry (night/dawn wandering is the key scenario) + an owned BLE **immersion tag** for the in-water event; geese-in-the-pool as the low-stakes bonus. Leads with the layers-of-protection doctrine (a 4-sided fence ~83% risk reduction) and the verbatim "never replaces a fence, alarm, or watching adult" line. |
| [`canary_curbwatch_research.md`](./canary_curbwatch_research.md) | **Canary Curbwatch research dossier (concept)** — a solar "child near the street" **awareness aid** (⚠️ warns an adult, *not* a barrier), modeled on roadside wildlife-detection: an always-on **FMCW radar** directional boundary-plane crossing (toward-street + dwell) wakes a **Vision Pro** starlight camera to confirm child-class. Honest about the >90% false-positive/desensitization trap, that children are the hardest class to detect (bias to sensitivity), and the verbatim "never replaces fences, gates, and supervision" line. |
| [`canary_gatekeeper_research.md`](./canary_gatekeeper_research.md) | **Canary Gatekeeper research dossier (concept)** — a stick-on, ultra-low-power solar gate-open witness: an **LIS3DH** accelerometer (open-angle from tilt + a climb/rattle bonus) or a **reed switch** (cheapest binary) → `ContactStateChange`, with "left open too long" via absence-inference. Notably, a **multi-year primary cell often beats solar** here because it dodges the no-charge-below-0 °C problem. Almost pure reuse of Fence Guard + the alert relay; no new vocabulary. |
| [`canary_herald_research.md`](./canary_herald_research.md) | **Canary Herald research dossier (concept)** — a plug-in **e-paper kitchen placard**, the display family's first e-paper member (the standard's "e-ink tile"): the household's taped-up note, writable from your phone over the existing local MQTT command idiom, rendering in ~a second via fast partial refresh — sharing the face with a calm AD-Core status line ("all 6 Canaries reporting · as of ~7:10 am") and the hub's retained weather. Plug-in on purpose so the radio is always awake — push, not poll; no mic, no camera, no glow, notes stamped in 10-minute buckets and auto-expiring, one anonymous **"got it" button** as the crumple gesture. Designed against the graveyard (TRMNL's fee, Tidbyt's cloud death, ad-serving smart displays) under an explicit contract: **lose to the sticky note nowhere** — one-minute one-person setup, zero accounts for readers, no subscription and no sunsettable server *structurally*. The original **outdoor door-placard** variant (honest disclosure face, never-advertise-absence rule, chime-bypass power, cold/UV limits) is kept as a deferred postscript. |
| [`canary_vehicle_can.md`](./canary_vehicle_can.md) | **Canary Vehicle — passive CAN bus witness** — the hero feature (ignition on/off → a fleet-wide arrival/departure claim, its own dedicated `ClaimKind`), why it's read-only by design, and the SocketCAN hardware path. Backed by real shipped code: `src/adapter/can_bus.rs` + the `adapter_host` SocketCAN reader — unit-tested, not yet bench-validated against a real vehicle. |
| [`canary_vehicle_profiles.md`](./canary_vehicle_profiles.md) | **Canary Vehicle — multi-brand DBC-sourced signal profiles** — a real, sourced (opendbc, MIT) signal matrix instead of hand-guessed byte offsets, a drift-checked tool (`scripts/dbc_signal_resolve.py`) that resolves signals instead of hand-computing them, and honestly-tiered profiles for Honda Pilot/Odyssey, Toyota Corolla, and Volkswagen MQB. |
| [`vehicle_dbc/SOURCES.md`](./vehicle_dbc/SOURCES.md) | Provenance + sync policy for the vendored DBC excerpts backing the vehicle profiles above. |
| [`canary_car_mode.md`](./canary_car_mode.md) | **Canary Car Mode — retired** — superseded by Canary Vehicle Guard. Kept as an honest record of why: Car Mode sensed the car *running with you in it* and drew ignition-USB power that's dead exactly when a parked-car witness is needed. The Meshtastic `vehicle_arrival_departure` code path survives as a plain capability. |
| [`canary_guardian_research.md`](./canary_guardian_research.md) | **Canary Guardian research dossier (concept)** — a worn duress witness on the low-power XIAO nRF52840 + Wio-SX1262 kit: press-and-hold panic beacon over off-grid mesh, "I'm OK" check-in with absence-inferred alerting, coarse anchor-proximity zone presence (the anti-AirTag). Honest about Meshtastic's best-effort limits, why nRF52 over ESP32, and why fall detection is a prompt not a claim. |
| [`canary_vehicle_guard_research.md`](./canary_vehicle_guard_research.md) | **Canary Vehicle Guard research dossier (concept)** — the parked-car witness that supersedes Car Mode: an onboard IMU wakes on motion (theft/tow) and a low-rate LoRa beacon carries a coarse find-my-car last-seen, on always-hot/battery power (not ignition-USB). Why talking to the car (BLE/OBD) is a dead end, why hot-car occupancy belongs to Canary Sense's radar, and the IMU-vs-CAN split with Canary Vehicle. |
| [`bom_pipeline.md`](./bom_pipeline.md) | **How the BOMs run themselves** — design intent (the CSVs) vs fetched supply-chain facts (`pricing.json`), the nightly Digi-Key/Mouser snapshot workflow, the exception policy (out-of-stock / price-jump / lifecycle issues), credentials setup, and the local runbook. |
| [`bom_pipeline_setup.md`](./bom_pipeline_setup.md) | **Hook it in — the setup guide** (radical honesty edition): the two free API keys step by step, the three repo secrets, the first-run checklist with the healthy log, the full failure truth table (every way it can break and what actually happens), what's CI-verified vs designed-but-awaiting-first-live-contact, and how anyone can adopt the pattern. |
| [`fcc_board_status.md`](./fcc_board_status.md) | **The per-board FCC lookup record** required by strategy doc 29 §10 before any copy claims a board is certified: FCC IDs found so far (XIAO ESP32-S3/Sense and ESP32-C3, grant conditions unread), every other board explicitly not-looked-up, and the standing rule that nothing here unlocks a radio SKU — those stay gated behind a Part 15B SDoC per SKU. |
| [`bom_canary_wap.csv`](./bom_canary_wap.csv) | Machine-readable BOM — Canary WAP (XIAO ESP32-S3 Sense). |
| [`bom_canary_vision.csv`](./bom_canary_vision.csv) | Machine-readable BOM — Canary Vision (ESP32 host + Grove Vision AI V2). |
| [`bom_canary_sense.csv`](./bom_canary_sense.csv) | Machine-readable BOM — Canary Sense (MR60BHA2 60 GHz kit + XIAO ESP32-C6). |
| [`bom_canary_display.csv`](./bom_canary_display.csv) | Machine-readable BOM — Canary Display (watch & dash variants). |

## The display family (watch station & dash)

The Canary Display's design record, from platform vision to bring-up:

| Document | Description |
|----------|-------------|
| [`display_platform_vision.md`](./display_platform_vision.md) | Why the family has a face — the display platform's north star. |
| [`display_trailblazer_spec.md`](./display_trailblazer_spec.md) | The trailblazer device spec — first hardware to carry the vision. |
| [`display_ux_design.md`](./display_ux_design.md) | Screen-by-screen UX design. |
| [`display_character.md`](./display_character.md) | The canary character — how the mascot behaves and why. |
| [`display_living_canary.md`](./display_living_canary.md) | The living-canary mood engine — honest moods mirroring system health. |
| [`display_care_wave.md`](./display_care_wave.md) | The care wave — gentle presence signals between households. |
| [`display_sound_clearance.md`](./display_sound_clearance.md) | Canary Voice acoustic clearance — every signature original and unencumbered, and provably not confusable with a regulated fire/CO alarm cadence (CI-guarded). |
| [`display_nightstand.md`](./display_nightstand.md) | The nightstand ("watch") variant — sleep, glanceability, bedside manners. |
| [`display_nightstand_line.md`](./display_nightstand_line.md) | **The Nightstand Line — three new display boards** (Waveshare ESP32-C6/S3 1.47" 172×320 + ESP32-S3 7" 800×480 touch): the hardware bring-up (ST7789 34-px offset, WS2812 ambient LED, GT911 + CH422G), color-as-primary language with the RGB LED as the across-the-room attention beacon, the honest night-light two-channel split (state-glow stays dark-when-safe; a summoned warm light is separate), the new 172×320 portrait layout, the 7" Dash-reuse + real 5-point touch, and the staged build plan. Board `pins.h` landed; HAL/emulator/registry are the next slice. |
| [`display_onboarding.md`](./display_onboarding.md) | On-glass onboarding flows. |
| [`display_settings.md`](./display_settings.md) | On-glass settings — what's adjustable without an app. |
| [`display_discovery_and_resilience.md`](./display_discovery_and_resilience.md) | Discovery & resilience — finding sensors, surviving outages. |
| [`tv_display_design.md`](./tv_display_design.md) | **Canary TV** — a hub-free ambient security surface for any television: the no-hub `/api/glass` path, the Seeed reTerminal/TRMNL competitive picture, and the "Canary TV" HDMI-dongle proposal. Reference page served at `/tv` ([`canary-display/tv/`](../../firmware/projects/canary-display/tv/)). |
| [`fleet_link_bench_checklist.md`](./fleet_link_bench_checklist.md) | Fleet link — hardware bench smoke-test gate for the direct BLE presence/GATT channel. |
| [`canary_wap_wifi.md`](./canary_wap_wifi.md) | How the Canary WAP's WiFi works — the AP→STA link + provisioning, single-radio coexistence, and WiFi *as a sensor* (CSI presence + probe-request counting). |
| [`display_bench_bringup.md`](./display_bench_bringup.md) | Display bench bring-up — from bare board to a beating face. |
| [`dev_playground_43b.md`](./dev_playground_43b.md) | **Dev playground (bench mode)** — the Waveshare 4.3B as a safe guided peripheral test bench: doorbell/intrusion/light/cap-touch/ToF/beam-gap plus RS485·Modbus and CAN·TWAI stations, the PG1 comms standard, and the fully-loaded pin tracker. |
| [`dev_playground_todo.md`](./dev_playground_todo.md) | **Dev playground — TODO & maintainer handoff** — where every piece of the drift-lock chain lives (firmware ↔ generator ↔ json ↔ sim ↔ test ↔ website carry), the exact ritual to add a station, the gotchas, and the next-station + bench-activation TODO. Start here to continue the playground. |
| [`display_modes.md`](./display_modes.md) | **Display modes — one glass, five gears** — the built, compile-gated mode system: fleet / bench / demo / debug / arcade, the no-bloat contract, host-tested cores + gear runtimes (`src/mode/`), per-mode policy (network/OTA/watchdog), entry/exit choreography, the `-dash-modes`/`-watch-modes` CI envs, and the browser twin behind the public `/modes` page. |
| [`display_peripheral_catalog.md`](./display_peripheral_catalog.md) | **Display peripheral catalog (4.3B)** — the curated what-plugs-in ledger, by wiring surface (DI/DO/I²C/RS485/CAN/radio): why each peripheral, what it's for, honest status, the "not this board" list (cameras/RTSP → where they really live), and the ATM-style combination plays. |
| [`display_mic_variant.md`](./display_mic_variant.md) | **The mic-bearing dash (Waveshare 4.3C)** — the listening contract: alarm patterns only (smoke T3 / CO T4), scalars-not-samples with the buffer zeroed per frame, off by default, hard mute = driver uninstalled, and the amber MIC chip that provably equals the driver state (host-tested invariant). A distinct privacy surface with its own board, env, and OTA product. |
| [`display_usability_protocol.md`](./display_usability_protocol.md) | **Usability protocol — testing the promises on real people** — the post-flash counterpart to the bench runbooks: think-aloud task scripts for every claim (first light with no phone, glance comprehension, half-asleep ack/mute, night manners, the mode gears, the demo storyline, the 4.3C "is it listening?" probes, proof-on-glass), scoring thresholds, a results ledger, and the serial-grammar appendix. |
| [`board_capability_map_43b.md`](./board_capability_map_43b.md) | **Board capability map (4.3B)** — an honest, cited ledger of every capability the dash board can do vs. what the firmware drives today, and for each unused one the exact feature gate + bench step to activate it (evidence vault, isolated DI/DO, RS485/Modbus, CAN, microSD, RTC/battery). |
| [`board_43b_activation_bench.md`](./board_43b_activation_bench.md) | **4.3B peripheral activation bench checklist** — the executable companion to the capability map: per capability (field I/O, RS485/Modbus, CAN/TWAI, evidence vault, RTC/battery), the exact wiring, build/flag, pass signal (the `[FIELD]`/`[RS485]`/`[CAN]` log lines), and the honesty-correction/`VERIFY` note each pass retires. |
| [`canary_ac_infinity_uis_research.md`](./canary_ac_infinity_uis_research.md) | **AC Infinity UIS research dossier (concept)** — can a Canary switch an AC-ADA3 UIS Control Plug and *know* the load came on? What the UIS port actually is (a 10 V rail + presence line + 0–10 V dim line, **not** USB — ⚠️ never plug a UIS cable into a Canary's USB-C jack), why there is no command set at the plug, the verified AC Infinity cloud API where the commands really live, an opto-isolated drive circuit, and why a CT clamp — not `loadState` — is what lets the device make an honest claim. Includes the bench protocol that turns the speculative half into measurements. |
| [`display_research.md`](./display_research.md) | Display hardware research notes. |
| [`waveshare_board_reference.md`](./waveshare_board_reference.md) | **Waveshare 4.3″ dash boards — our reference, kept fresh** — our first-party reference for the 4.3 / 4.3B / 4.3C hardware (pin maps, onboard silicon, the CH422G "switched" EXIO bits, interfaces, mechanicals), backed by a machine-readable vendor-facts snapshot (`board_facts.json`) that a weekly freshness loop re-pulls from the canonical Waveshare wiki and drift-locks against our `pins.h`. Facts are ours to read and can't go stale; the vendor page stays canonical. |

The CSVs use a flat, RoHS-style schema (enforced by `scripts/lint_bom.py`):

```
Item,RefDes,Qty,Required,Category,Description,Manufacturer,MPN,Mouser,DigiKey,LCSC,UnitUSD,ExtUSD,Lifecycle,RoHS,Notes
```

The CSVs carry *design intent*; live distributor SKUs, stock, price breaks
and lifecycle are fetched nightly into [`pricing.json`](./pricing.json) —
see [`bom_pipeline.md`](./bom_pipeline.md). Prices typed in the CSVs are
indicative seed values only.

> **Pin definitions are authoritative in firmware**, not here — see
> [`firmware/boards/`](../../firmware/boards/). This area documents the hardware
> *build*; the pin map uses the firmware defaults, but check the
> **Firmware-Support column** in the build plan — some peripherals (RGB LED,
> tamper) are pin-defined but not yet driven by code.

For getting a finished device online, see
[`../getting_started_canary.md`](../getting_started_canary.md).
